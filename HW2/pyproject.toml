[project]
name = "mlops-hw2"
version = "0.1.0"
description = "MLOPS-26, домашняя работа 2"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.4",
    "transformers>=4.51",
    "accelerate>=1.0",
    "numpy>=2.1",
    "pyyaml>=6.0",
    "psutil>=6.0",
    "peft>=0.14",
    "matplotlib>=3.9",
]

[tool.uv]
package = false
